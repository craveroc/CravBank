package com.cravero.cravbank.common;

public enum ErrorCode {
    VALIDATION_ERROR,
    UNAUTHORIZED,
    FORBIDDEN,
    NOT_FOUND,
    CONFLICT,
    INVITATION_INVALID,
    REFRESH_TOKEN_INVALID,
    INTERNAL_ERROR
}
